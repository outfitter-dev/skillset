---
name: team-guidance
---

# Team Guidance

When working in this repo:

- Treat `skillset/` as the source of truth.
- Edit source files first, then run `skillset check` before rendering output.
- Preview generated output with `skillset build` before writing it.
- Keep generated Claude and Codex files repo-local until a human chooses how to
  activate them.
