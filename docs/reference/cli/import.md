---
description: The Skillset import command can import provider-native skills or plugins into source.
---
<!-- Generated by `bun run docs:generate`; do not edit directly. -->

# `skillset import`

> Generated from Skillset's CLI presentation contract. See the [documentation system](../../development/documentation-system.md).

Import provider-native skills or plugins into source.

## Usage

```text
skillset import <path> [--kind <skill|skills|plugin|plugins>] [--from <provider>] [--name <name>] [--json] [--root <path>]
skillset import <claude|codex|cursor|agents> [--json] [--root <path>]
```

## Options

| Option | Meaning |
| --- | --- |
| `--from <value>` | Select the provider origin for local import. |
| `--json` | Emit exactly one versioned finite JSON result document. |
| `--kind <value>` | Select the import source kind. |
| `--name <value>` | Set a route-specific human or stable name where the route permits it. |
| `--root <value>` | Select the repository root; defaults to cwd or Git root according to the route. |
