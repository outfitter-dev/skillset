---
description: The Skillset reconcile command can reconcile a managed source/output conflict.
---
<!-- Generated by `bun run docs:generate`; do not edit directly. -->

# `skillset reconcile`

> Generated from Skillset's CLI presentation contract. See the [documentation system](../../development/documentation-system.md).

Reconcile a managed source/output conflict.

## Usage

```text
skillset reconcile [managed-path] [--use <source|output>] [--yes] [--json] [--root <path>]
```

## Options

| Option | Meaning |
| --- | --- |
| `--json` | Emit exactly one versioned finite JSON result document. |
| `--root <value>` | Select the repository root; defaults to cwd or Git root according to the route. |
| `--use <value>` | Select source or output as the authority for a reconciliation plan. |
| `--yes` | Confirm a fully specified plan-first mutation without prompting. |

## Examples

```text
skillset reconcile
skillset reconcile .claude/skills/demo/SKILL.md --use output
```
