---
description: The Skillset release command can audit, plan, apply, and amend releases.
---
<!-- Generated by `bun run docs:generate`; do not edit directly. -->

# `skillset release`

> Generated from Skillset's CLI presentation contract. See the [documentation system](../../development/documentation-system.md).

Audit, plan, apply, and amend releases.

## `skillset release amend`

Amend applied release history.

### Usage

```text
skillset release amend <@ref> [--ref <ref>] [--reason <text>|--reason-file <path>|--reason -] [--json] [--root <path>]
```

### Options

| Option | Meaning |
| --- | --- |
| `--json` | Emit exactly one versioned finite JSON result document. |
| `--reason <value>` | Provide change or release reason text, with '-' meaning stdin. |
| `--reason-file <value>` | Read change or release reason text from a file. |
| `--ref <value>` | Select a change or release record by reference. |
| `--root <value>` | Select the repository root; defaults to cwd or Git root according to the route. |

## `skillset release apply`

Apply a planned release.

### Usage

```text
skillset release apply [--yes] [--json] [--root <path>]
```

### Options

| Option | Meaning |
| --- | --- |
| `--json` | Emit exactly one versioned finite JSON result document. |
| `--root <value>` | Select the repository root; defaults to cwd or Git root according to the route. |
| `--yes` | Confirm a fully specified plan-first mutation without prompting. |

## `skillset release audit`

Audit release and changelog state.

### Usage

```text
skillset release audit [--json] [--root <path>]
```

### Options

| Option | Meaning |
| --- | --- |
| `--json` | Emit exactly one versioned finite JSON result document. |
| `--root <value>` | Select the repository root; defaults to cwd or Git root according to the route. |

## `skillset release plan`

Plan versions and changelog updates.

### Usage

```text
skillset release plan [--json] [--root <path>]
```

### Options

| Option | Meaning |
| --- | --- |
| `--json` | Emit exactly one versioned finite JSON result document. |
| `--root <value>` | Select the repository root; defaults to cwd or Git root according to the route. |
