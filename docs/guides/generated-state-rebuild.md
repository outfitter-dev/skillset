---
description: Rebuilds pre-v3 generated state for inherent Agent standards without granting old locks cleanup authority.
---

# Rebuild Generated State for Inherent Standards

Skillset 0.27.0 makes every adopted, applicable Agent standard an inherent projection. Existing provider targets remain provider choices, but valid instruction, skill, and plugin source may add `AGENTS.md`, `.agents/skills/`, or `plugins/<plugin>/agents/` output without a standards selector.

This is a one-time generated-state rebuild, not a source migration. Preserve `skillset.yaml`, `.skillset/`, provider-native source, user edits, and unmanaged neighbors. Do not add `compile.agents` or another opt-out. Pre-v3 locks can explain why a rebuild is required, but they cannot prove ownership or authorize deletion.

## Stop Conditions

Stop before moving or rebuilding anything when:

- the repository is dirty and the owner has not classified the changes;
- a proposed generated root contains authored or uncertain files;
- a lock is malformed, from the future, or disagrees with the configured output roots;
- the canonical source does not pass `skillset check`; or
- the required released Skillset version is unavailable.

Do not delete a mixed directory, recursively replace a repository root, or infer ownership from a pre-v3 lock. Move only individually reviewed generated paths to a recoverable backup outside every configured output root.

## Evidence to Capture

Before the rebuild, record:

```bash
git -C /absolute/path/to/repository rev-parse HEAD
git -C /absolute/path/to/repository status --short
bunx @skillset/cli@0.27.0 --version
bunx @skillset/cli@0.27.0 status --json --root /absolute/path/to/repository
bunx @skillset/cli@0.27.0 check --only outputs --root /absolute/path/to/repository
```

For pre-v3 state, the last two commands should report that the lock is rebuild-only and cannot authorize cleanup. Save the complete diagnostic. Record each configured root, each path the owner classifies as generated, the backup location, and the repository owner accepting that classification.

After moving only those reviewed paths, rebuild from canonical source and verify the new ownership model:

```bash
bunx @skillset/cli@0.27.0 build --root /absolute/path/to/repository
bunx @skillset/cli@0.27.0 build --yes --root /absolute/path/to/repository
bunx @skillset/cli@0.27.0 check --only outputs --root /absolute/path/to/repository
bunx @skillset/cli@0.27.0 status --json --root /absolute/path/to/repository
bunx @skillset/cli@0.27.0 diff --root /absolute/path/to/repository
git -C /absolute/path/to/repository status --short
```

Save and inspect the unconfirmed build plan before running the `--yes` command. The receipt must identify the repository and source commit, Skillset version, pre-rebuild diagnostic, backup, preview, generated diff, v3 locks, standard physical owners and logical provider consumers, relevant disposable consumer proof, owner acceptance, date, and limitations. Retain the backup until the owner accepts the rebuilt result.

Package publication and active CLI upgrades are separate release operations. Do not run these commands against an unreleased `0.27.0`, and do not treat a source checkout or development binary as proof of released-version migration.

## Known Repository Inventory

This inventory records the September 13, 2026 pre-release state. Refresh every row immediately before its owner-approved rebuild.

| Repository | Canonical source | Current generated state | Rebuild boundary |
| --- | --- | --- | --- |
| `/Users/mg/Developer/outfitter/skillset` | `skillset.yaml`, `.skillset/` | pre-v3 root and `plugins/` locks generated by `skillset@0.1.0`; candidates include the three provider configuration/rule files, two nested `AGENTS.md` files, the generated source changelog, and six `plugins/skillset/{claude,codex,cursor}/` manifest or skill paths. The checkout also contains an untracked `.agents/notes/2026-09-07-lead-takeover/` directory. | Released-version follow-up only. Stop while the checkout is dirty. After its owner classifies that note and every generated path, back up only approved compiler output and review the newly inherent `.agents/skills/use-skillset/` and `plugins/skillset/agents/` trees plus standard ownership on shared `AGENTS.md` paths. |
| `/Users/mg/Developer/galligan/skills` | `skillset.yaml`, `.skillset/` | pre-v3 `skillset.lock` and `skills/skillset.lock`; observed output candidates are `.claude/CLAUDE.md`, `AGENTS.md`, and `skills/{agentish-styleguide,minority-report,people-words}/` | Released-version follow-up only. Matt must approve the exact generated classification before any move. Preserve the custom Codex `skills/` provider root and review the additional inherent `.agents/skills/` standard root independently. |
| `/Users/mg/Developer/outfitter/agent-workbench` | `skillset.yaml`, `.skillset/` | pre-v3 root and `plugins/` locks; observed candidates are `.claude/.gitkeep`, `.codex/.gitkeep`, and the six `plugins/ultralite/{claude,codex}/` manifest or skill paths | Released-version follow-up only. Classify the complete `plugins/ultralite/` tree before moving individual generated paths, then review inherent `.agents/skills/{ultralite-agents,ultralite-audit}/` and `plugins/ultralite/agents/` output. |
| `/Users/mg/Developer/outfitter/trails` | `skillset.yaml`, `.skillset/` | pre-v3 root, `.claude/skills/`, and `.agents/skills/` locks; observed candidates are five project-agent files plus the 16 mirrored skill trees under each skill root | Released-version follow-up only. Preserve the distinction between project-agent files and skills. Review `.agents/skills/` as a shared standard-owned physical root with the Codex provider consumer after the rebuild. |
| `/Users/mg/Developer/tinker/composed` | `skillset.yaml`, `.skillset/` | pre-v3 root and `.claude/skills/` locks; observed candidates are `.claude/.gitkeep` and `.claude/skills/composed-conventions/` | Released-version follow-up only. Back up only the two reviewed generated candidates, then review the additional inherent `.agents/skills/composed-conventions/` standard tree. |

Exact move plans for Skillset, Agent Workbench, Trails, and Composed remain owner-blocked follow-up evidence. Their pre-v3 locks identify candidates but cannot authorize moving them. The owner must classify these exact candidates before a script is written or run:

- Skillset: `skillset.lock`, `plugins/skillset.lock`, `.claude/settings.json`, `.codex/hooks/hooks.json`, `.cursor/rules/fixtures.mdc`, `.skillset/plugins/skillset/CHANGELOG.md`, `apps/skillset/src/__tests__/AGENTS.md`, `fixtures/AGENTS.md`, and the `plugins/skillset/{claude,codex,cursor}/` trees.
- Agent Workbench: `skillset.lock`, `plugins/skillset.lock`, `.claude/.gitkeep`, `.codex/.gitkeep`, `plugins/ultralite/claude/`, and `plugins/ultralite/codex/`.
- Trails: `skillset.lock`, `.claude/skills/`, `.agents/skills/`, `.claude/agents/{clark,lewis,maintainer}.md`, and `.codex/agents/{clark,lewis}.toml`.
- Composed: `skillset.lock`, `.claude/.gitkeep`, and `.claude/skills/`.

After approval, use the same fail-fast, durable-backup pattern as Galligan Skills with the repository's absolute root and these individually accepted paths. Until that receipt exists, do not replace the owner decision with a broad directory move or a lock-derived cleanup plan.

The reviewed SET-409/SET-411 implementation worktree has current v3 locks and may run its checked-out development command for pull-request verification:

```bash
bun ./apps/skillset/src/cli.ts build --yes --root .
bun ./apps/skillset/src/cli.ts check --only outputs --root .
bun ./apps/skillset/src/cli.ts status --json --root .
bun ./apps/skillset/src/cli.ts diff --root .
```

That verifies the source branch and its worktree-local generated bytes. It does not rebuild the pre-v3 canonical checkout, prove npm publication, perform a released-version downstream rebuild, install globally, change user configuration, grant trust, or activate a runtime.

## Galligan Skills Follow-Up

After `skillset@0.27.0` is published and Matt accepts the inventory, create a durable recoverable backup outside the repository and every configured output root, preflight all seven sources and destinations, and move only the reviewed generated candidates:

```bash
set -euo pipefail
galligan_repo=/Users/mg/Developer/galligan/skills
galligan_backup=/Users/mg/skillset-rebuild-backups/galligan-skills-0.27.0
test ! -e "$galligan_backup"
test -f "$galligan_repo/.claude/CLAUDE.md"
test -f "$galligan_repo/AGENTS.md"
test -f "$galligan_repo/skillset.lock"
test -d "$galligan_repo/skills/agentish-styleguide"
test -d "$galligan_repo/skills/minority-report"
test -d "$galligan_repo/skills/people-words"
test -f "$galligan_repo/skills/skillset.lock"
mkdir -p "$galligan_backup/.claude" "$galligan_backup/skills"
chmod 700 "$galligan_backup"
mv "$galligan_repo/.claude/CLAUDE.md" "$galligan_backup/.claude/CLAUDE.md"
mv "$galligan_repo/AGENTS.md" "$galligan_backup/AGENTS.md"
mv "$galligan_repo/skillset.lock" "$galligan_backup/skillset.lock"
mv "$galligan_repo/skills/agentish-styleguide" "$galligan_backup/skills/agentish-styleguide"
mv "$galligan_repo/skills/minority-report" "$galligan_backup/skills/minority-report"
mv "$galligan_repo/skills/people-words" "$galligan_backup/skills/people-words"
mv "$galligan_repo/skills/skillset.lock" "$galligan_backup/skills/skillset.lock"
```

The shell exits on the first failed preflight or move. If a move fails after an earlier path was moved, stop and restore every already-moved path from the durable backup before retrying; do not continue into the build. After all moves succeed, run the released-version preview, inspect it, and then run the confirmed build and verification commands above with `--root /Users/mg/Developer/galligan/skills`. Compare each provider tree and the new `.agents/skills/` standard tree with the backup, run the pinned individual-skills consumer proof, and retain the backup until Matt accepts the result.

## Recovery

If the rebuilt output is wrong, stop before making further changes. Restore only from the recorded backup after checking that the current destination still matches the just-generated bytes and that no newer edits would be overwritten. A rebuild receipt is evidence for review; it never broadens authority to publish, install globally, change provider settings, trust a plugin, or activate it.
