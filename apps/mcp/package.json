{
  "name": "@skillset/mcp",
  "version": "0.0.1",
  "description": "MCP server for skillset (future)",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/types/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "types": "bunx tsc -p tsconfig.json --emitDeclarationOnly",
    "build": "bun run clean && bun build ./src/index.ts --outdir dist --target bun && bun run types"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@skillset/core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "^1.3.2",
    "typescript": "^5.6.3"
  },
  "author": "Matt Galligan",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/outfitter-dev/skillset.git",
    "directory": "apps/mcp"
  }
}
