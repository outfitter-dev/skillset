Match the skill to what SET-599 and SET-600 actually built: read the printed verdicts (clean, output-missing, source-ahead, output-edited, diverged) instead of re-deriving them, resolve authored conflicts before rerunning skillset resolve --yes, and check that the repository's own compiler is the one answering.

Bump: minor
Scope: skill:skillset-fix
