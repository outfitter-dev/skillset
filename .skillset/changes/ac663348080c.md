Generate individually installable Agent Skills from self-contained plugin source while retaining package-contained skills when dependencies or skill-local hooks block standalone publication.

Bump: minor
Group: SET-529
Scope: plugin:skillset
