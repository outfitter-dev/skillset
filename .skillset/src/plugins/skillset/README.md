# Skillset Plugin

This generated plugin teaches agents how to use the local `skillset` compiler.

Source lives under `.skillset/src/plugins/skillset`. Generated plugin bundles are written to `plugins-claude/` and `plugins-codex/`.
