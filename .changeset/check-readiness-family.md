---
"skillset": minor
---

Consolidate source diagnostics, generated-output verification, and CI readiness under `skillset check`, with explicit local write, narrow output, and CI modes. Remove the former top-level `lint`, `verify`, and `ci` commands without compatibility aliases.
