---
"skillset": patch
---

Split existing-directory `skillset init` from named child-repository `skillset create`, add grouped detected-source selection and registry-derived provider defaults, scaffold canonical workspace or plugin instruction files, and add explicit plus guided schema-validated adaptive-hook authoring with searchable registry-derived events, compatibility, and source-unit attachments.
