---
"skillset": patch
---

Split existing-directory `skillset init` from named child-repository `skillset create`, add grouped detected-source selection and registry-derived provider defaults, scaffold canonical workspace or plugin instruction files, and add schema-validated, registry-derived adaptive-hook scaffolding with source-unit attachments.
