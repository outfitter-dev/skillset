---
'skillset': patch
---

Add a global immutable operational report store, schema-backed receipts, secure sandbox export, and skillset report show retrieval. Align `workspace.cacheKey` schema validation with the existing 160-character operational identity boundary.
