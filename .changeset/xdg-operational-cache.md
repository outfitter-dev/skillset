---
"@skillset/core": patch
"skillset": patch
---

Back Skillset operational cache paths with the repo XDG cache bucket while preserving logical `.skillset/cache/...` paths in reports, locks, isolated builds, tests, and generated guidance.
