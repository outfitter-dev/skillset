---
"skillset": patch
---

Keep `skillset create` workflow guidance in canonical `.skillset/rules/` source so the first confirmed build can generate `AGENTS.md` without an unmanaged-output collision.
