---
"@skillset/core": patch
---

Point adaptive hook feature-registry evidence at the canonical hooks documentation after consolidating the separate adaptive hooks page, and expose adaptive hook units through the `skillset lookup hooks adaptive` lens.
