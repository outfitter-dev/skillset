---
"skillset": minor
---

Complete the pre-1.0 CLI hard cut across runtime flags, hooks, active documentation, fixtures, and generated Skillset guidance. Bare `dev` now watches in preview mode and `dev --write` enables continuous writes; retired commands, flags, and environment vocabulary are guarded from maintained surfaces.
