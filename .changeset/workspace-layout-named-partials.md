---
"skillset": patch
---

Use root `skillset.yaml` plus `.skillset/` as the only authoring workspace layout, and add first-class named partials with workspace/plugin resolution, ambiguity diagnostics, and recursion cycle checks.
