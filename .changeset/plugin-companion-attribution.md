---
"skillset": patch
---

Report shared plugin package companions (`README.md`, `assets/`, `scripts/`, `src/`) under the consumers that actually own them, such as the Agent Plugins standard profile, instead of attributing them to the first enabled provider target.
