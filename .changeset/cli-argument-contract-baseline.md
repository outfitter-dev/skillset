---
"skillset": patch
---

Reconcile CLI help and maintained argument contracts, including explicit reference flags and test status/tail grammar; reject previously ignored `lookup --root` and `hooks print --root` flags; constrain the hidden test worker to its internal protocol grammar; and make undeclared-option diagnostics command-owned instead of leaking global option-family wording.
