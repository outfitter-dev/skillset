---
"skillset": patch
---

Reconcile CLI help and maintained argument contracts, reject previously ignored `lookup --root` and `hooks print --root` flags, and constrain the hidden test worker to its internal protocol grammar.
