---
"@skillset/schema": patch
"skillset": patch
---

Add `skillset change migrate` for converting legacy frontmatter pending entries to reason-only ledger entries, and demote the `change-entry` schema docs to compatibility-only recovery guidance.
