---
"@skillset/cli": minor
"skillset": minor
---

Publish the complete Bun-targeted CLI as `@skillset/cli`, keep the transitional unscoped bundle byte-identical, and expose `skillset --version` as a shared distribution smoke contract.
