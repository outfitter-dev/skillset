---
"skillset": patch
---

Update `skillset init` and `skillset create` scaffolds for the unified `.skillset/src` layout. Setup now creates placeholders for the main source families (`agents`, `hooks`, `plugins`, `rules`, `shared`, `skills`, `_claude`, and `_codex`) by default, and `--include` is reserved for the optional CI workflow.
