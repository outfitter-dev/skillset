---
"skillset": patch
---

Refresh workspace-layout diagnostics and adoption reporting so current ordinary and dedicated Skillset workspaces are described with the 1.0 workspace marker language instead of legacy config paths.
