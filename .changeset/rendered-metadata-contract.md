---
"skillset": patch
---

Replace rendered skill `metadata.generated` stamps with the artifact version and `skillset.schema` rendered-contract marker, and stop injecting generic compiler metadata into rendered agents.
