---
"skillset": minor
---

Remove maintainer-only provider evidence maintenance from the shipped CLI and expose it only through repository-owned `providers:check`, `providers:diff`, and `providers:update` scripts.
