---
"skillset": minor
---

Cut setup and import flows to the 1.0 workspace layout: `init` now scaffolds ordinary `.skillset/skillset.yaml` workspaces, `create` defaults to dedicated `skillset.yaml`/`skillset/` repos, and imports resolve the active source root.
