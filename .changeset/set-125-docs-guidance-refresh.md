---
"skillset": patch
---

Refresh adopter-facing docs and self-hosted Skillset guidance for the derive/render/destination vocabulary, and rename the feature-registry `loweringOwner` field to `renderOwner` and the `lowering-outcomes` feature id to `render-results` (feature doc renamed to `docs/features/render-results.md`). Historical ADRs and the deterministic-projection concept are deliberately preserved.
