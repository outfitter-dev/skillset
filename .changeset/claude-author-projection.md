---
"skillset": patch
---

Render canonical author metadata into Claude's supported object shape, report omitted author fields explicitly, and remove unsupported marketplace provenance. Native plugin authors that cannot form canonical source now block import and surface an `unreadable-plugin-author` adoption diagnostic instead of disappearing.
