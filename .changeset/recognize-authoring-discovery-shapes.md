---
"skillset": minor
---

Recognize skill groups and drafts during discovery, reject duplicate skill leaves, classify reserved rule path segments, and report the new metadata through `list` and `explain` without flattening rendered paths.
