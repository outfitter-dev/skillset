---
"skillset": patch
---

`skillset draft --yes` no longer reports the shipped skill it copied from as a written path; the write count and JSON `writes` list only the draft, the ledger, and generated output.
