---
"skillset": patch
---

Apply aggregate CI package Changeset enforcement only to workspaces that opt into Changesets with `.changeset/config.json`, so downstream authoring repositories can commit README updates without dummy npm release entries.
