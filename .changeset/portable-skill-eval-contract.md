---
"skillset": minor
---

Add portable skill-local eval declarations, shared validation, and read-only `skillset eval list` target-matrix inspection. Scaffold compatible eval source with `skillset new --preset evals`.
