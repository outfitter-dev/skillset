---
"@skillset/core": patch
---

Record Claude hook event, matcher, finite matcher value, and event-specific handler semantics in the provider capability registry; route native validation, lookup event output, and ignored matcher diagnostics through the selected provider's registry entry.
