---
"skillset": minor
---

Flatten Skillset pending change entries from `changes/pending/*.md` to `changes/*.md`, keeping applied history, release records, and release state as JSON/JSONL ledger files in the same committed change directory while leaving generated-output provenance in `.skillset.lock`.
