---
"skillset": patch
---

Add `skillset init --layout root` so existing repositories can explicitly opt into the root `skillset.yaml` plus `skillset/` source layout while keeping nested `.skillset/` as the default init scaffold.
