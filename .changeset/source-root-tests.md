---
"skillset": patch
---

Move deterministic test declarations into active source-root `tests.yaml` files and replace the retired `source`/`assertions` shape with selector-driven `select` and `checks` declarations.
