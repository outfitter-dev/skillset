---
"skillset": patch
---

Generate checked-in Skillset schema reference artifacts and maximal examples from the shared schema contract package, and scaffold workspace manifests with a YAML language-server schema reference.
