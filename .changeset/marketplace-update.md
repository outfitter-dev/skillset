---
"@skillset/core": patch
"skillset": patch
---

Add `skillset marketplace update` to render provider-supported marketplace indexes from Skillset marketplace source, refresh marketplace provenance in the existing root `skillset.lock`, and support mixed local/external Claude catalog entries through derived git-subdirectory sources.
