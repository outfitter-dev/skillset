---
"skillset": minor
---

Add plugin project-use draft `only` and `override` modes with selection-first pairing, deterministic effective names, lifecycle-safe cleanup, and inspectable lock, explain, and status provenance.
