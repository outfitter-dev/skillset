---
"skillset": patch
---

Switch interactive prompt rendering and searchable multiselect behavior to Clack while preserving prompt defaults, cancellation, injected streams, and non-interactive command behavior.
