---
"skillset": patch
---

Switch interactive prompt rendering to Clack, refine the guided init language and plan, and add an adoption-intent step before the flat source picker while preserving prompt defaults, cancellation, injected streams, and non-interactive command behavior.
