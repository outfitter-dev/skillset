---
"skillset": minor
---

Copy plugin skills selected by `plugins.internal_use` into fixed project skill roots with deterministic collision names, boolean internal markers, owned cleanup, and inspectable provenance.
