---
"@skillset/core": patch
"@skillset/schema": patch
"skillset": patch
---

Include Cursor in the default provider target plan so omitted `compile.targets` builds Claude, Codex, and Cursor outputs by default.
