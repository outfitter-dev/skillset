---
"skillset": patch
---

Keep shared references literal inside Markdown code spans that cross line breaks, so `skillset build` no longer rewrites or copies a reference written inside a multiline backtick span.
