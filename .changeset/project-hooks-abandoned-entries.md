---
"skillset": patch
---

Remove the project SessionStart entry Skillset previously wrote when its target is disabled or its `projectRoot` moves, instead of leaving the command active and unmanaged.
