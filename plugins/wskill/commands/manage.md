---
description: "Manage alias mappings and pin skills"
---

Interactively lists discovered skills, lets you assign explicit aliases, and pin deterministic mappings into `.claude/wskill/config.json`. Use this when ambiguous `w/<alias>` tokens are reported.
