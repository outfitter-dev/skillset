---
description: "Diagnose collisions, missing skills, and stale cache"
---

Reads `events.jsonl` logs for unmatched or ambiguous invocations, shows missing skill paths, and recommends pins or namespace disambiguation. Also validates cache staleness and TTL.
