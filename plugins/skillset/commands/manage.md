---
description: "Manage alias mappings and pin skills"
---

Interactively lists discovered skills, lets you assign explicit aliases, and pin deterministic mappings into `.claude/skillset/config.json`. Use this when ambiguous `$<alias>` tokens are reported.
