---
description: "Manage alias mappings and pin skills"
---

Interactively lists discovered skills, lets you assign explicit aliases, and pin deterministic mappings into `.skillset/config.yaml`. Use this when ambiguous `$<alias>` tokens are reported.
