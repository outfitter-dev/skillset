# Skillset Plugin

This generated plugin teaches agents how to use the local `skillset` compiler.

Source lives under `.skillset/plugins/skillset`. Generated manifests and shared package content are written once under `plugins/skillset/`.
