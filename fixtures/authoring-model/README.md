# Authoring model fixture

This fixture exercises the current source locations introduced by SET-551: `RULES.md`, `subagents/`, and `shared/partials/` at workspace and plugin scope.

The grouping directories, `_drafts/`, and bracketed rule directories are carried for later issues. SET-585 owns grouping, draft, and reserved-segment discovery; SET-554 owns flattened plugin-skill placement. Tests here deliberately skip those future behaviors. The future root `plugins:` configuration belongs to SET-552 and is not present.
