# Authoring model fixture

This fixture exercises the current source locations introduced by SET-551: `RULES.md`, `subagents/`, and `shared/partials/` at workspace and plugin scope.

SET-585 activates plain and parenthesized skill groups, `_drafts/`, and exact reserved `[.]` and `[...]` rule segments while keeping other bracketed or parenthesized rule directories literal. SET-552 adds the root `plugins.internal_use` selection and default package-path plan. SET-554 owns the later project-copy rendering and flattened plugin-skill placement.
