# Authoring model fixture

This fixture exercises the current source locations introduced by SET-551: `RULES.md`, `subagents/`, and `shared/partials/` at workspace and plugin scope.

SET-585 activates plain and parenthesized skill groups, `_drafts/`, and exact reserved `[.]` and `[...]` rule segments while keeping other bracketed or parenthesized rule directories literal. SET-554 owns flattened plugin-skill placement. The future root `plugins:` configuration belongs to SET-552 and is not present.
