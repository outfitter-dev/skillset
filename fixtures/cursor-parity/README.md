# Cursor parity fixture

This fake repository captures the Cursor output Skillset produces at the
SET-550 evidence baseline. Its literal bracket and parenthesis directories are
intentional. Provider claims not settled by pinned Cursor documentation remain
named `test.todo` cases in `cursor-parity.test.ts`.
